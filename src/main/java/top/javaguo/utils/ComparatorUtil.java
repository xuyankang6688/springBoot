package top.javaguo.utils;

import java.util.Comparator;

public class ComparatorUtil implements Comparator {
    public int compare(Object object1, Object object2) {// 实现接口中的方法
     /*   OrderInfo p1 = (OrderInfo) object1; // 强制转换
        OrderInfo p2 = (OrderInfo) object2;
        return new Double(p1.getPaymentAmount()).compareTo(new Double(p2.getPaymentAmount()));*/
     return 0;
    }
}
